//Crie um programa que utilize a declaração switch, sem switch statement especificado.

package main

import "fmt"

func main() {
	x := 2
	switch {
	case x == 1:
		fmt.Println(x)
	case x == 2:
		fmt.Println(x)
	case x == 3:
		fmt.Println(x)
	}
}
